---
- name         : Install and configure alfred
  hosts        : all
  gather_facts : yes
  connection   : "{{ kitchen_connection | default('local') }}"
  #vars_files   :
  #               - "alfred_pkg_vars.yml"
  vars         :
                 alfred_gps: true
                 alfred_vis: true
  roles        :
                 - "ansible-alfred"